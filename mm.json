{
	"description": "An app-config file, specific for testing how to customise mongo-migration features when executed as a composer package",
	"defaults": {
		"authorization": {
			"file": "./auth.json"
		},
		"migrations": {
			"path": "/opt/mongo-migrations/dev/tests/migrations",
			"entries": "mm_migration_log"
		}
	}
}